# After Chapter 4 — Checkpoint

No new packages at the checkpoint. Carries forward chapters 1–3 and adds:

- `scripts/06_briefing_summary.R` — builds the status paragraph with
  `dplyr`, `lubridate`, and `glue`, and writes it to
  `outputs/tables/briefing_summary.txt`.

This is the full set of scripts the book has produced so far. Run them in
order, 01 through 06, and the empty folders fill in with real artifacts:
a cleaned dataset, two figures, and a briefing summary.

`data/raw/` still holds only the downloaded CSV (not duplicated here);
`data/processed/` and `outputs/` stay empty until you run the scripts.
