# After Chapter 0 — Orientation

Folder scaffold only, matching `create_project_scaffold()`. No code written
yet — the reader has their workspace set up and the raw CSV downloaded into
`data/raw/` (not duplicated in this snapshot).
